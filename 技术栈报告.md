# 《数据库原理与应用》智能学习平台技术栈报告

**项目名称**：SQL语言学习平台  
**开发团队**：  
**版本**：1.0.0  

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构设计](#2-系统架构设计)
3. [技术栈详解](#3-技术栈详解)
4. [后端技术实现](#4-后端技术实现)
5. [前端技术实现](#5-前端技术实现)
6. [数据库设计](#6-数据库设计)
7. [AI智能功能实现](#7-ai智能功能实现)
8. [安全机制](#8-安全机制)
9. [部署架构](#9-部署架构)
10. [核心功能实现原理](#10-核心功能实现原理)
11. [技术亮点与创新](#11-技术亮点与创新)

---

## 1. 项目概述

### 1.1 项目背景

本项目是一个面向数据库课程教学的 **SQL语言学习平台**，旨在为学生提供一个交互式的SQL学习环境。平台集成了用户管理、在线SQL练习、AI智能辅导、社区交流等功能，实现了"学、练、问、评"一体化的学习体验。

### 1.2 核心功能模块

| 功能模块 | 描述 |
|---------|------|
| 用户认证系统 | 注册、登录、JWT令牌验证 |
| SQL例题练习 | 基于真实数据库的SQL练习与自动评判 |
| AI智能辅导 | 代码分析、错误引导、答案评价 |
| 知识库系统 | SQL知识点学习与进度追踪 |
| 社区评论 | 用户交流、问答、AI自动回复 |
| SQL练习场 | 自由执行SQL语句的沙箱环境 |

### 1.3 技术架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端 (浏览器)                           │
│                    Vue 3 + Vite + Pinia                         │
└─────────────────────────────┬───────────────────────────────────┘
                              │ HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     后端服务 (Node.js)                           │
│                 Express + RESTful API                           │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐      │
│  │  认证    │  评论    │  知识库   │  例题    │   AI     │      │
│  │  模块    │  模块    │  模块    │  模块    │  服务    │      │
│  └────┬─────┴────┬─────┴────┬─────┴────┬─────┴────┬─────┘      │
└───────┼──────────┼──────────┼──────────┼──────────┼─────────────┘
        │          │          │          │          │
        ▼          ▼          ▼          ▼          ▼
┌───────────────────────┐              ┌────────────────────────┐
│   NeonDB PostgreSQL   │              │    阿里云通义千问 API   │
│     云数据库          │              │   (qwen3-coder-plus)    │
└───────────────────────┘              └────────────────────────┘
```

---

## 2. 系统架构设计

### 2.1 前后端分离架构

本项目采用 **前后端分离** 的现代Web开发架构：

- **前端**：Vue 3 单页应用 (SPA)，通过 Axios 与后端 API 通信
- **后端**：Node.js + Express 构建的 RESTful API 服务
- **数据库**：PostgreSQL 云数据库 (NeonDB)
- **AI服务**：阿里云通义千问大模型 API

### 2.2 MVC 设计模式

后端采用经典的 **MVC (Model-View-Controller)** 设计模式：

```
backend/src/
├── config/          # 配置层 - 环境变量、数据库配置
├── models/          # Model层 - 数据模型与数据库操作
├── controllers/     # Controller层 - 业务逻辑处理
├── routes/          # 路由层 - API路由定义
├── middleware/      # 中间件 - 认证、验证、日志
├── services/        # 服务层 - AI服务、第三方集成
└── scripts/         # 脚本 - 数据库初始化
```

### 2.3 组件化前端架构

前端采用 Vue 3 的 **Composition API** 和组件化架构：

```
frontend/src/
├── views/           # 页面级组件
├── components/      # 可复用组件
├── stores/          # Pinia状态管理
├── services/        # API服务封装
├── router/          # 路由配置
└── assets/          # 静态资源
```

---

## 3. 技术栈详解

### 3.1 后端技术栈

| 技术/框架 | 版本 | 用途 |
|-----------|------|------|
| **Node.js** | v18+ | JavaScript运行时环境 |
| **Express** | 4.18.2 | Web应用框架，处理HTTP请求 |
| **PostgreSQL** | 15+ | 关系型数据库 |
| **pg** | 8.11.3 | PostgreSQL Node.js驱动 |
| **jsonwebtoken** | 9.0.2 | JWT令牌生成与验证 |
| **bcryptjs** | 2.4.3 | 密码加密 |
| **express-validator** | 7.0.1 | 请求参数验证 |
| **helmet** | 7.1.0 | HTTP安全头设置 |
| **cors** | 2.8.5 | 跨域资源共享 |
| **express-rate-limit** | 7.1.5 | API请求频率限制 |
| **uuid** | 9.0.1 | UUID生成 |
| **dotenv** | 16.3.1 | 环境变量管理 |
| **nodemon** | 3.0.2 | 开发热重载 |

### 3.2 前端技术栈

| 技术/框架 | 版本 | 用途 |
|-----------|------|------|
| **Vue.js** | 3.4.0 | 渐进式JavaScript框架 |
| **Vite** | 5.0.8 | 新一代前端构建工具 |
| **Vue Router** | 4.2.5 | 官方路由管理器 |
| **Pinia** | 2.1.7 | Vue 3状态管理库 |
| **Axios** | 1.6.2 | HTTP客户端 |
| **marked** | 11.1.0 | Markdown解析器 |
| **highlight.js** | 11.9.0 | 代码语法高亮 |
| **dayjs** | 1.11.10 | 日期时间处理 |

### 3.3 云服务与部署

| 服务 | 提供商 | 用途 |
|------|--------|------|
| **数据库** | NeonDB | PostgreSQL云数据库 |
| **AI模型** | 阿里云 | 通义千问大模型API |
| **前端部署** | Netlify/Vercel | 静态网站托管 |
| **后端部署** | Render | Node.js服务托管 |
| **内网穿透** | ngrok | 本地开发外网访问 |

---

## 4. 后端技术实现

### 4.1 Express应用架构

Express应用的核心配置（`app.js`）：

```javascript
const express = require('express');
const cors = require('cors');
const helmet = require('helmet');
const rateLimit = require('express-rate-limit');

const app = express();

// 安全中间件
app.use(helmet({
  crossOriginResourcePolicy: { policy: "cross-origin" }
}));

// CORS配置 - 允许跨域请求
app.use(cors({
  origin: true,
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
  allowedHeaders: ['Content-Type', 'Authorization']
}));

// 请求体解析
app.use(express.json({ limit: '10mb' }));

// API路由注册
app.use('/api/auth', authRoutes);
app.use('/api/exercises', exerciseRoutes);
app.use('/api/comments', commentRoutes);
app.use('/api/knowledge', knowledgeRoutes);
app.use('/api/ai', aiRoutes);
```

### 4.2 数据库连接池

使用 `pg` 模块创建 PostgreSQL 连接池，实现高效的数据库访问：

```javascript
const { Pool } = require('pg');

const dbConfig = {
  connectionString: process.env.DATABASE_URL,
  ssl: { rejectUnauthorized: false },
  max: 20,                    // 最大连接数
  idleTimeoutMillis: 30000,   // 空闲超时
  connectionTimeoutMillis: 10000  // 连接超时
};

const pool = new Pool(dbConfig);

// 封装查询方法
const query = async (text, params) => {
  const start = Date.now();
  const result = await pool.query(text, params);
  const duration = Date.now() - start;
  console.log(`📊 查询执行: ${duration}ms, 行数: ${result.rowCount}`);
  return result;
};
```

**技术要点**：
- 使用连接池避免频繁创建/销毁连接
- SSL加密保证数据传输安全
- 查询日志便于性能监控

### 4.3 RESTful API设计

API遵循 RESTful 设计规范：

| HTTP方法 | 路径 | 描述 |
|---------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录 |
| GET | `/api/exercises` | 获取例题列表 |
| POST | `/api/exercises/:id/submit` | 提交答案 |
| POST | `/api/exercises/execute` | 执行SQL |
| POST | `/api/ai/analyze` | AI代码分析 |

**响应格式统一规范**：

```javascript
// 成功响应
{
  "success": true,
  "data": { ... },
  "message": "操作成功"
}

// 错误响应
{
  "success": false,
  "message": "错误描述"
}
```

### 4.4 中间件机制

#### 4.4.1 JWT认证中间件

```javascript
const authenticateToken = async (req, res, next) => {
  const authHeader = req.headers.authorization;
  const token = authHeader && authHeader.split(' ')[1]; // Bearer TOKEN

  if (!token) {
    return res.status(401).json({
      success: false,
      message: '未提供认证令牌'
    });
  }

  try {
    const decoded = jwt.verify(token, config.jwtSecret);
    const user = await User.findById(decoded.userId);
    req.user = user;
    next();
  } catch (error) {
    if (error.name === 'TokenExpiredError') {
      return res.status(401).json({ message: '令牌已过期' });
    }
    return res.status(401).json({ message: '无效的认证令牌' });
  }
};
```

#### 4.4.2 请求频率限制

```javascript
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15分钟窗口
  max: 100,                  // 最多100次请求
  message: {
    success: false,
    message: '请求过于频繁，请稍后再试'
  }
});

// AI路由特殊限制（更严格）
const aiLimiter = rateLimit({
  windowMs: 60 * 1000,  // 1分钟窗口
  max: 10               // 最多10次请求
});
```

---

## 5. 前端技术实现

### 5.1 Vue 3 Composition API

本项目全面采用 Vue 3 的 **Composition API**，相比 Options API 具有更好的代码组织和类型推断：

```javascript
<script setup>
import { ref, onMounted, computed } from 'vue'
import { exerciseAPI } from '@/services/api'
import { useAuthStore } from '@/stores/auth'

// 响应式状态
const exercises = ref([])
const loading = ref(true)
const selectedExercise = ref(null)

// 计算属性
const completedCount = computed(() => 
  exercises.value.filter(e => e.is_correct).length
)

// 生命周期钩子
onMounted(async () => {
  loading.value = true
  const response = await exerciseAPI.getAll()
  exercises.value = response.data.data.exercises
  loading.value = false
})
</script>
```

### 5.2 Pinia状态管理

Pinia 是 Vue 3 官方推荐的状态管理库，替代了 Vuex：

```javascript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import api from '@/services/api'

export const useAuthStore = defineStore('auth', () => {
  // 状态
  const user = ref(null)
  const token = ref(localStorage.getItem('token') || null)

  // 计算属性
  const isLoggedIn = computed(() => !!token.value && !!user.value)

  // 登录方法
  const login = async (credentials) => {
    const response = await api.post('/auth/login', credentials)
    if (response.data.success) {
      user.value = response.data.data.user
      setToken(response.data.data.token)
      return { success: true }
    }
    return { success: false, message: response.data.message }
  }

  return { user, token, isLoggedIn, login }
})
```

**Pinia的优势**：
- 更简洁的API设计
- 完整的TypeScript支持
- 支持Composition API风格
- 开发者工具集成

### 5.3 Vue Router路由配置

```javascript
import { createRouter, createWebHistory } from 'vue-router'

// 路由懒加载 - 按需加载组件
const Exercises = () => import('@/views/Exercises.vue')
const Knowledge = () => import('@/views/Knowledge.vue')

const routes = [
  {
    path: '/exercises',
    name: 'Exercises',
    component: Exercises,
    meta: { title: '例题练习' }
  },
  {
    path: '/profile',
    name: 'Profile',
    component: Profile,
    meta: { title: '个人中心', requiresAuth: true }  // 需要登录
  }
]

// 路由守卫 - 权限控制
router.beforeEach(async (to, from, next) => {
  const authStore = useAuthStore()
  
  if (to.meta.requiresAuth && !authStore.isLoggedIn) {
    next({ name: 'Login', query: { redirect: to.fullPath } })
  } else {
    next()
  }
})
```

### 5.4 Axios请求封装

```javascript
import axios from 'axios'

// 创建axios实例
const api = axios.create({
  baseURL: '/api',
  timeout: 30000,
  headers: { 'Content-Type': 'application/json' }
})

// 请求拦截器 - 自动添加Token
api.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => Promise.reject(error)
)

// 响应拦截器 - 统一错误处理
api.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      // Token过期，清除登录状态
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)
```

### 5.5 动态背景Canvas动画

使用 HTML5 Canvas 实现粒子动画效果：

```javascript
class Particle {
  constructor() {
    this.x = Math.random() * width
    this.y = Math.random() * height
    this.size = Math.random() * 1.5 + 0.2
    this.speedX = Math.random() * 0.4 - 0.2
    this.speedY = Math.random() * 0.5 + 0.1
    this.opacity = Math.random() * 0.6 + 0.1
    this.color = `rgba(255, 215, 0, ${this.opacity})`  // 金色粒子
  }

  update() {
    this.x += this.speedX
    this.y += this.speedY
    this.x += Math.sin(this.y * 0.01) * 0.1  // 正弦摆动效果
  }

  draw() {
    ctx.beginPath()
    ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2)
    ctx.fillStyle = this.color
    ctx.fill()
  }
}

// 动画循环
const animate = () => {
  ctx.clearRect(0, 0, width, height)
  particles.forEach(p => {
    p.update()
    p.draw()
  })
  requestAnimationFrame(animate)
}
```

---

## 6. 数据库设计

### 6.1 用户系统表

```sql
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  uuid VARCHAR(36) UNIQUE NOT NULL,
  username VARCHAR(50) UNIQUE NOT NULL,
  email VARCHAR(100) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  avatar_url VARCHAR(255),
  role VARCHAR(20) DEFAULT 'user',      -- user/admin/moderator
  is_active BOOLEAN DEFAULT true,
  last_login TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 6.2 例题系统表

```sql
-- 例题表
CREATE TABLE exercises (
  id SERIAL PRIMARY KEY,
  title VARCHAR(200) NOT NULL,
  description TEXT NOT NULL,
  difficulty VARCHAR(20) DEFAULT 'beginner',  -- beginner/intermediate/advanced
  category VARCHAR(50),
  hint TEXT,
  correct_sql TEXT NOT NULL,
  expected_result_description TEXT,
  order_index INT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 学生答题记录表
CREATE TABLE exercise_submissions (
  id SERIAL PRIMARY KEY,
  user_id INT REFERENCES users(id),
  exercise_id INT REFERENCES exercises(id),
  user_sql TEXT NOT NULL,
  is_correct BOOLEAN DEFAULT FALSE,
  ai_feedback TEXT,
  attempt_count INT DEFAULT 1,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 6.3 练习数据库表

```sql
-- 学生表 (教学用)
CREATE TABLE practice_student (
  Sno CHAR(10) PRIMARY KEY,
  Sname CHAR(20) UNIQUE,
  Ssex CHAR(2),
  Sage INT,
  Sdept CHAR(20)
);

-- 课程表 (教学用)
CREATE TABLE practice_course (
  Cno CHAR(4) PRIMARY KEY,
  Cname CHAR(40) NOT NULL,
  Cpno CHAR(4),
  Ccredit SMALLINT
);

-- 选课成绩表 (教学用)
CREATE TABLE practice_sc (
  Sno CHAR(10),
  Cno CHAR(4),
  Grade INT,
  PRIMARY KEY (Sno, Cno)
);
```

### 6.4 评论系统表

```sql
CREATE TABLE comments (
  id SERIAL PRIMARY KEY,
  user_id INT REFERENCES users(id),
  parent_id INT REFERENCES comments(id),  -- 回复关联
  content TEXT NOT NULL,
  code_snippet TEXT,                       -- SQL代码片段
  is_ai_reply BOOLEAN DEFAULT false,       -- AI回复标识
  likes_count INT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 6.5 ER图

```
┌─────────────┐       ┌─────────────────────┐
│   users     │       │     comments        │
├─────────────┤       ├─────────────────────┤
│ id (PK)     │◄──────│ user_id (FK)        │
│ username    │       │ parent_id (FK)      │──┐
│ email       │       │ content             │  │
│ password    │       │ code_snippet        │  │
└─────────────┘       └─────────────────────┘◄─┘
      │
      │
      ▼
┌─────────────────────┐       ┌─────────────────┐
│ exercise_submissions│       │   exercises     │
├─────────────────────┤       ├─────────────────┤
│ user_id (FK)        │       │ id (PK)         │
│ exercise_id (FK)    │──────►│ title           │
│ user_sql            │       │ correct_sql     │
│ is_correct          │       │ difficulty      │
│ ai_feedback         │       └─────────────────┘
└─────────────────────┘
```

---

## 7. AI智能功能实现

### 7.1 AI服务架构

本项目集成了**阿里云通义千问大模型**，采用双模型策略：

| 模型 | 用途 | 特点 |
|------|------|------|
| `qwen3-coder-plus` | SQL代码分析、纠错、例题评判 | 代码理解能力强 |
| `qwen-max` | 通用对话、提示生成、知识问答 | 对话流畅自然 |

### 7.2 AI服务类实现

```javascript
class AIService {
  constructor() {
    this.apiKey = config.qwenApiKey;
    this.apiUrl = config.qwenApiUrl;
    this.coderModel = 'qwen3-coder-plus';  // 代码模型
    this.maxModel = 'qwen-max';            // 对话模型
  }

  async callQwenAPI(messages, maxTokens = 2000, modelType = 'default') {
    let model;
    switch (modelType) {
      case 'coder': model = this.coderModel; break;
      case 'max': model = this.maxModel; break;
      default: model = this.defaultModel;
    }

    const response = await fetch(this.apiUrl, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${this.apiKey}`
      },
      body: JSON.stringify({
        model: model,
        messages: messages,
        max_tokens: maxTokens,
        temperature: 0.7
      })
    });

    const data = await response.json();
    return data.choices[0].message.content;
  }
}
```

### 7.3 AI功能应用场景

#### 7.3.1 SQL代码分析

```javascript
async analyzeSQLCode(sqlCode) {
  const messages = [
    {
      role: 'system',
      content: `你是一位专业的SQL教师和代码评审专家。请分析用户提供的SQL代码，指出：
1. 代码的正确性和可能的错误
2. 代码风格和最佳实践建议
3. 性能优化建议
4. 常见易错点提醒`
    },
    {
      role: 'user',
      content: `请分析以下SQL代码：\n\`\`\`sql\n${sqlCode}\n\`\`\``
    }
  ];

  return this.callQwenAPI(messages, 2000, 'coder');
}
```

#### 7.3.2 例题答案评判与引导

```javascript
async analyzeExerciseAnswer(exerciseDescription, hint, userSQL, userResult, correctSQL) {
  const messages = [
    {
      role: 'system',
      content: `你是一位耐心的SQL教师，正在帮助学生练习SQL查询。学生的答案不正确，你需要：
1. 分析错误原因：指出学生SQL语句中的具体问题
2. 引导思考：不要直接给出正确答案，而是通过提问或提示引导学生思考
3. 给出建议：提供修改方向，但让学生自己尝试修正
4. 鼓励学生：用友好的语气鼓励学生继续尝试

注意：不要直接展示正确的SQL语句，用循序渐进的方式引导。`
    },
    {
      role: 'user',
      content: `**题目要求**：${exerciseDescription}
**题目提示**：${hint}
**学生的SQL语句**：${userSQL}
**学生的查询结果**：${JSON.stringify(userResult.rows)}`
    }
  ];

  return this.callQwenAPI(messages, 1500, 'coder');
}
```

#### 7.3.3 智能评论回复

```javascript
async generateReply(commentContent, parentContent = null) {
  const messages = [
    {
      role: 'system',
      content: `你是《数据库原理与应用》智能学习平台的AI助手"小Q"。你的任务是回复用户的评论，提供有帮助的SQL学习建议。
回复要求：
1. 友好、专业、鼓励学习
2. 如果用户提问，给出清晰的解答
3. 如果用户分享经验，给予肯定并补充
4. 适当引用SQL知识点`
    },
    { role: 'user', content: commentContent }
  ];

  return this.callQwenAPI(messages, 1000, 'max');
}
```

### 7.4 Prompt工程技巧

本项目在AI提示词设计上采用了以下技巧：

1. **角色设定**：明确定义AI的角色（SQL教师、代码专家）
2. **输出格式**：指定回复的结构（分点、使用Markdown）
3. **行为约束**：限制AI行为（不直接给答案，引导思考）
4. **语气要求**：指定语言风格（友好、专业、鼓励）
5. **上下文提供**：提供完整的问题背景信息

---

## 8. 安全机制

### 8.1 密码安全

使用 **bcryptjs** 进行密码哈希，采用12轮加盐：

```javascript
const bcrypt = require('bcryptjs');

// 密码加密存储
const passwordHash = await bcrypt.hash(password, 12);

// 密码验证
const isValid = await bcrypt.compare(plainPassword, hashedPassword);
```

### 8.2 JWT令牌认证

```javascript
const jwt = require('jsonwebtoken');

// 生成Token
const generateToken = (userId) => {
  return jwt.sign(
    { userId },
    config.jwtSecret,
    { expiresIn: '7d' }  // 7天过期
  );
};

// 验证Token
const decoded = jwt.verify(token, config.jwtSecret);
```

### 8.3 SQL注入防护

使用**参数化查询**防止SQL注入：

```javascript
// ✅ 安全：参数化查询
const sql = 'SELECT * FROM users WHERE email = $1';
const result = await query(sql, [email]);

// ❌ 危险：字符串拼接
const sql = `SELECT * FROM users WHERE email = '${email}'`;
```

### 8.4 SQL执行安全沙箱

例题模块的SQL执行器实现了安全沙箱：

```javascript
static async executeUserSQL(userSQL) {
  // 只允许SELECT语句
  const trimmedSQL = userSQL.trim().toUpperCase();
  if (!trimmedSQL.startsWith('SELECT')) {
    return { success: false, error: '只允许执行 SELECT 查询语句' };
  }

  // 检查危险关键字
  const dangerousKeywords = ['DROP', 'DELETE', 'UPDATE', 'INSERT', 'ALTER', 'TRUNCATE', 'CREATE'];
  for (const keyword of dangerousKeywords) {
    if (trimmedSQL.includes(keyword)) {
      return { success: false, error: `不允许使用 ${keyword} 操作` };
    }
  }

  // 安全执行
  return await query(userSQL);
}
```

### 8.5 HTTP安全头

使用 **Helmet** 中间件设置安全HTTP头：

```javascript
app.use(helmet({
  contentSecurityPolicy: true,           // CSP策略
  crossOriginEmbedderPolicy: true,       // 跨域嵌入策略
  crossOriginOpenerPolicy: true,         // 跨域打开策略
  crossOriginResourcePolicy: { policy: "cross-origin" },
  dnsPrefetchControl: true,              // DNS预取控制
  frameguard: true,                      // 点击劫持防护
  hidePoweredBy: true,                   // 隐藏X-Powered-By
  hsts: true,                            // 强制HTTPS
  xssFilter: true                        // XSS过滤
}));
```

### 8.6 请求频率限制

```javascript
// 全局限制
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000,  // 15分钟
  max: 100                    // 最多100次
});

// AI接口更严格限制
const aiLimiter = rateLimit({
  windowMs: 60 * 1000,        // 1分钟
  max: 10                     // 最多10次
});
```

---

## 9. 部署架构

### 9.1 部署方案

```
┌─────────────────────────────────────────────────────────────┐
│                        用户访问                              │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          │                               │
          ▼                               ▼
┌─────────────────────┐       ┌─────────────────────┐
│   Netlify/Vercel    │       │      Render         │
│   (前端静态托管)     │       │   (后端Node.js)     │
│   Vue 3 SPA         │       │   Express API       │
└─────────┬───────────┘       └─────────┬───────────┘
          │                             │
          │         API请求              │
          └─────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          │                               │
          ▼                               ▼
┌─────────────────────┐       ┌─────────────────────┐
│      NeonDB         │       │    阿里云通义千问    │
│  PostgreSQL云数据库  │       │    大模型API        │
└─────────────────────┘       └─────────────────────┘
```

### 9.2 环境变量配置

```bash
# 数据库
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d

# AI模型
QWEN_API_KEY=sk-xxxxxxxxxxxxx
QWEN_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
QWEN_CODER_MODEL=qwen3-coder-plus
QWEN_MAX_MODEL=qwen-max

# 服务器
PORT=3000
NODE_ENV=production
```

### 9.3 Vite构建配置

```javascript
// vite.config.js
export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: { '@': '/src' }
  },
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true
      }
    }
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
    minify: 'terser'
  }
})
```

---

## 10. 核心功能实现原理

### 10.1 用户认证流程

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  用户   │────►│  前端   │────►│  后端   │────►│ 数据库  │
│         │     │         │     │         │     │         │
│ 输入账密 │     │ 发送请求 │     │ 验证密码 │     │ 查询用户 │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
                                    │
                          ┌─────────┴─────────┐
                          │ 生成JWT Token     │
                          │ 返回用户信息       │
                          └───────────────────┘
                                    │
                                    ▼
                          ┌───────────────────┐
                          │ 前端存储Token     │
                          │ localStorage      │
                          │ 后续请求携带Token │
                          └───────────────────┘
```

### 10.2 例题答案验证流程

```
用户提交SQL
     │
     ▼
┌───────────────────┐
│ 1. 安全检查       │
│   - 是否为SELECT  │
│   - 危险关键字    │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 2. 执行用户SQL    │
│   获取查询结果     │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 3. 执行正确答案   │
│   获取预期结果     │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 4. 比较结果集     │
│   规范化比较       │
└─────────┬─────────┘
          │
     ┌────┴────┐
     │         │
     ▼         ▼
  正确       错误
     │         │
     │         ▼
     │    ┌───────────────┐
     │    │ 5. 调用AI分析 │
     │    │   生成引导反馈 │
     │    └───────┬───────┘
     │            │
     ▼            ▼
┌───────────────────┐
│ 6. 保存答题记录   │
│   返回结果给用户   │
└───────────────────┘
```

### 10.3 结果集比较算法

```javascript
static compareResultSets(userRows, correctRows) {
  // 行数不同，直接返回false
  if (userRows.length !== correctRows.length) {
    return false;
  }

  // 规范化每一行数据
  const normalizeRow = (row) => {
    const values = Object.values(row).map(v => 
      v === null ? 'NULL' : String(v).trim()
    );
    return values.sort().join('|');  // 排序后拼接
  };

  // 转为Set进行无序比较
  const userSet = new Set(userRows.map(normalizeRow));
  const correctSet = new Set(correctRows.map(normalizeRow));

  // 比较两个Set
  for (const item of userSet) {
    if (!correctSet.has(item)) {
      return false;
    }
  }

  return true;
}
```

---

## 11. 技术亮点与创新

### 11.1 创新点总结

| 创新点 | 描述 |
|--------|------|
| **AI引导式教学** | 不直接给出答案，而是通过AI引导学生思考，符合教育理念 |
| **双模型策略** | 代码任务用专业代码模型，对话用通用模型，效果更佳 |
| **安全SQL沙箱** | 学生可以自由练习SQL，同时保证系统安全 |
| **实时答案验证** | 自动比对查询结果，即时反馈学习效果 |
| **动态背景效果** | Canvas粒子动画，提升用户体验 |
| **进度追踪** | 记录学习轨迹，支持个性化学习 |

### 11.2 技术优势

1. **现代化技术栈**：Vue 3 + Vite + Pinia，代表前端最新技术方向
2. **云原生架构**：数据库、AI服务、部署全部采用云服务
3. **安全设计**：JWT认证、bcrypt加密、SQL注入防护、请求限流
4. **可扩展性**：模块化设计，易于添加新功能
5. **用户体验**：响应式设计、动态效果、即时反馈

### 11.3 性能优化措施

1. **路由懒加载**：按需加载页面组件
2. **数据库连接池**：复用连接，减少开销
3. **请求限流**：防止资源滥用
4. **静态资源CDN**：Netlify/Vercel自带CDN加速

---

## 附录

### A. 项目目录结构

```
sql-learning-platform/
├── backend/                    # 后端服务
│   ├── src/
│   │   ├── config/            # 配置文件
│   │   │   ├── database.js    # 数据库配置
│   │   │   └── env.js         # 环境变量
│   │   ├── controllers/       # 控制器
│   │   │   ├── authController.js
│   │   │   ├── exerciseController.js
│   │   │   ├── commentController.js
│   │   │   └── aiController.js
│   │   ├── models/            # 数据模型
│   │   │   ├── User.js
│   │   │   ├── Exercise.js
│   │   │   └── Comment.js
│   │   ├── routes/            # 路由
│   │   ├── middleware/        # 中间件
│   │   ├── services/          # 服务
│   │   │   └── aiService.js   # AI服务
│   │   ├── scripts/           # 脚本
│   │   └── app.js             # 入口文件
│   ├── .env                   # 环境变量
│   └── package.json
├── frontend/                   # 前端应用
│   ├── src/
│   │   ├── views/             # 页面组件
│   │   ├── components/        # 通用组件
│   │   ├── stores/            # Pinia状态
│   │   ├── services/          # API服务
│   │   ├── router/            # 路由配置
│   │   ├── assets/            # 静态资源
│   │   └── main.js            # 入口文件
│   ├── public/                # 公共资源
│   ├── vite.config.js         # Vite配置
│   └── package.json
└── README.md
```

### B. API接口文档摘要

| 模块 | 接口 | 方法 | 描述 |
|------|------|------|------|
| 认证 | `/api/auth/register` | POST | 用户注册 |
| 认证 | `/api/auth/login` | POST | 用户登录 |
| 认证 | `/api/auth/profile` | GET | 获取个人信息 |
| 例题 | `/api/exercises` | GET | 获取例题列表 |
| 例题 | `/api/exercises/:id` | GET | 获取例题详情 |
| 例题 | `/api/exercises/execute` | POST | 执行SQL |
| 例题 | `/api/exercises/:id/submit` | POST | 提交答案 |
| AI | `/api/ai/analyze` | POST | 代码分析 |
| AI | `/api/ai/ask` | POST | 问答 |
| 评论 | `/api/comments` | GET/POST | 评论增删改查 |

---

**报告撰写日期**：2025年12月  
**技术支持**：

